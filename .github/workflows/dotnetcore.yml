name: .NET Core

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - uses: microsoft/msbuild@15.9.20
    - name: Build with msbuild
      run: msbuild
      shell: cmd
