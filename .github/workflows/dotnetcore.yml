name: .NET Core

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - uses: microsoft/msbuild@v15.9.20.62856
    - name: Build with msbuild
      run: msbuild
      shell: cmd
